"""Loaders e estruturas tipadas dos datasets do projeto.

`datasets` carrega o corpus curado de letras (`br_curated_lyrics.parquet`) e o
dataset Spotify bruto. `albums` extrai e indexa o catálogo de álbuns derivado
das tracks curadas.
"""
